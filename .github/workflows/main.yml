name: Deploy to GitHub Pages

on:
  push:
    branches: ["main", "master"]
    paths:
      # Chỉ chạy khi có thay đổi ở frontend hoặc chính workflow này
      - 'frontend/**'
      - '.github/workflows/main.yml'
  workflow_dispatch:   # cho phép chạy tay từ tab Actions (kể cả khi không đổi code)

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm install

      - name: Build
        run: npm run build
        env:
          # URL backend Azure, nhúng vào bundle lúc build (GitHub Pages là static).
          # .env.production ở local bị gitignore nên không tới được CI -> đặt ở đây.
          VITE_API_URL: https://hcmute-lms.azurewebsites.net/api/v1

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./frontend/dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4