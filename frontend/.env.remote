# Dùng bởi `npm run dev:remote` (nạp qua node --env-file): chạy frontend local
# nhưng mọi request /api/v1/* được proxy sang backend thật trên Azure.
# Đăng nhập bằng tài khoản seed: student@lms.com / password, v.v.
BACKEND_URL=https://hcmute-lms.azurewebsites.net
