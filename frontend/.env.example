# VITE_API_URL: Base URL of the backend REST API.
# Leave EMPTY for local dev — requests to "/api/v1" are served by the mock API in
# server.ts (npm run dev). For production, set this to the deployed backend, e.g.
# VITE_API_URL="https://hcmute-lms.azurewebsites.net/api/v1"
VITE_API_URL=""
